# Mapa de rotas

A rota de diagnóstico da arquitetura é `GET /api/v3/architecture/status`. As rotas atuais continuam registradas pelo `server.mjs`. O inventário completo deve ser atualizado a cada consolidação de módulo.
