CI P0/P1 Fusion — Final V1

Substitua somente:
.github/workflows/p0-auditoria.yml

O que muda:
- mantém npm ci
- mantém npm run check
- mantém npm run test:p0-auditoria
- acrescenta npm run test:p1-implantacao
- não usa secrets
- não chama Supabase real
- não altera runtime do Fusion

Recomendado: subir em branch para não disparar deploy do Render a partir do main.

Comandos:
git checkout -b ci/p1-implantacao-20260812
git add .github/workflows/p0-auditoria.yml
git commit -m "ci: incluir auditoria P1 de implantacao"
git push -u origin ci/p1-implantacao-20260812

Depois abra PR para main.
O evento pull_request executará o workflow antes do merge.

Se o Git disser que a branch já existe, use outro nome.
